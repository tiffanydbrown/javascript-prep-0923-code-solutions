const number1 = 8;
const number2 = 16;
const product = number1 * number2;
console.log(product, typeof product);

const charge = 78;
const payment = 40;
const amountRemaining = charge - payment;
console.log(amountRemaining, typeof amountRemaining);

const tests = 80;
const asssignments = 75;
const final = 95;
const grade = (tests + asssignments + final) / 3;
console.log(grade, typeof grade);

const firstName = 'Tiffany';
const lastName = 'Brown';
const fullName = firstName + ' ' + lastName;
console.log(fullName, typeof fullName);

const pH = 3;
const isAcidic = pH < 7;
console.log(isAcidic, typeof isAcidic);

const numberOfSoldiers = 100;
const isSparta = numberOfSoldiers === 300;
console.log(isSparta, typeof isSparta);

let nickname = 'Tiffany Brown';
nickname = nickname + ' is the GOAT';
console.log(nickname, typeof nickname);
